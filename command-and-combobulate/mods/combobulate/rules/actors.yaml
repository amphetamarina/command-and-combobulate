combobulate.terminal:
	Inherits: PDOX
	# RenderSprites.Image defaults to the actor name, so without this override
	# the engine looks for sequences under `combobulate.terminal` instead of `pdox`
	# and the production palette crashes refreshing the cameo.
	RenderSprites:
		Image: pdox
	Buildable:
		Queue: Building
		BuildPaletteOrder: 5
		# PDOX requires Allied tech and is capped at one; override so the Terminal
		# is always available and uncapped.
		Prerequisites: ~techlevel.infonly
		BuildLimit: 0
		Description: actor-combobulate-terminal.description
	Valued:
		Cost: 100
	Tooltip:
		Name: actor-combobulate-terminal.name
	# PDOX draws -200 power and hosts the Chronosphere superweapon. Strip both
	# so the Terminal is a plain building.
	Power:
		Amount: 0
	-ChronoshiftPower@chronoshift:
	-SupportPowerChargeBar:
	-InfiltrateForSupportPowerReset:
	-ProvidesPrerequisite@germany:
	-ProvidesPrerequisite@germanstructure:
	-ProvidesPrerequisite@buildingname:
	-MustBeDestroyed:
	CombobulateTerminalRef:
	CombobulateBuiltTerminal:
	# Context-window readout over the terminal; the bridge sets the text and
	# reddens it as the agent's context fills (the base browns out).
	CombobulateLabel:
		Font: Bold
		Position: Top
		ValidRelationships: Ally, Neutral, Enemy
		Color: 9EE6A0

combobulate.folderwall:
	Inherits: SBAG
	RenderSprites:
		Image: sbag
	Interactable:
	CombobulateLabel:
		Font: Bold
		Position: Top
		ValidRelationships: Ally, Neutral, Enemy
		Color: FFFFAA

combobulate.agent:
	Inherits: HELI
	RenderSprites:
		Image: heli
	# The agent flies (fast, and straight over folder walls) and hovers where it
	# is working. It stays Neutral with HoldFire stance and is never given an
	# attack order, and its ammo never depletes (it never fires), so it neither
	# fights nor flies off to a helipad to rearm; a helicopter's idle behaviour
	# is to hover in place. Only Buildable is dropped (its prerequisites do not
	# exist in this mod, and the bridge spawns the agent directly).
	-Buildable:
	Aircraft:
		Speed: 170
	CombobulateLabel:
		Position: Top
		ValidRelationships: Ally, Neutral, Enemy
		Color: FFE066

combobulate.agent.codex:
	Inherits: ARTY
	RenderSprites:
		Image: arty
	Mobile:
		Speed: 170
	CombobulateLabel:
		Position: Top
		ValidRelationships: Ally, Neutral, Enemy
		Color: FF99CC

combobulate.subagent:
	Inherits: E1
	RenderSprites:
		Image: e1
	Mobile:
		Speed: 120
	CombobulateLabel:
		Position: Top
		ValidRelationships: Ally, Neutral, Enemy
		Color: FFE066

# One small civilian house per touched file. The label (file name + size) is
# always shown so every touched file is named in-world without clicking.
combobulate.file1:
	Inherits: V05
	RenderSprites:
		Image: v05
	CombobulateLabel:
		Font: TinyBold
		Position: Top
		RequiresSelection: false
		ValidRelationships: Ally, Neutral, Enemy
		Color: FFFFFF

combobulate.file2:
	Inherits: V06
	RenderSprites:
		Image: v06
	CombobulateLabel:
		Font: TinyBold
		Position: Top
		RequiresSelection: false
		ValidRelationships: Ally, Neutral, Enemy
		Color: FFFFFF

combobulate.file3:
	Inherits: V07
	RenderSprites:
		Image: v07
	CombobulateLabel:
		Font: TinyBold
		Position: Top
		RequiresSelection: false
		ValidRelationships: Ally, Neutral, Enemy
		Color: FFFFFF

combobulate.file4:
	Inherits: V08
	RenderSprites:
		Image: v08
	CombobulateLabel:
		Font: TinyBold
		Position: Top
		RequiresSelection: false
		ValidRelationships: Ally, Neutral, Enemy
		Color: FFFFFF

combobulate.file5:
	Inherits: V09
	RenderSprites:
		Image: v09
	CombobulateLabel:
		Font: TinyBold
		Position: Top
		RequiresSelection: false
		ValidRelationships: Ally, Neutral, Enemy
		Color: FFFFFF

combobulate.file6:
	Inherits: V10
	RenderSprites:
		Image: v10
	CombobulateLabel:
		Font: TinyBold
		Position: Top
		RequiresSelection: false
		ValidRelationships: Ally, Neutral, Enemy
		Color: FFFFFF

combobulate.file7:
	Inherits: V11
	RenderSprites:
		Image: v11
	CombobulateLabel:
		Font: TinyBold
		Position: Top
		RequiresSelection: false
		ValidRelationships: Ally, Neutral, Enemy
		Color: FFFFFF

# Iconic role buildings: a touched file's kind reads from its silhouette -- a
# barracks for tests, a power plant for config, a tech center for a manifest, a
# radar dome for docs, a silo for build output. They are spawned as inert
# neutral decorations: Buildable is dropped (no production here) and Power is
# zeroed so they do not perturb a power grid.
combobulate.file.test:
	Inherits: TENT
	RenderSprites:
		Image: tent
	-Buildable:
	CombobulateLabel:
		Font: TinyBold
		Position: Top
		RequiresSelection: false
		ValidRelationships: Ally, Neutral, Enemy
		Color: FFFFFF

combobulate.file.config:
	Inherits: POWR
	RenderSprites:
		Image: powr
	-Buildable:
	Power:
		Amount: 0
	CombobulateLabel:
		Font: TinyBold
		Position: Top
		RequiresSelection: false
		ValidRelationships: Ally, Neutral, Enemy
		Color: FFFFFF

combobulate.file.manifest:
	Inherits: ATEK
	RenderSprites:
		Image: atek
	-Buildable:
	Power:
		Amount: 0
	CombobulateLabel:
		Font: TinyBold
		Position: Top
		RequiresSelection: false
		ValidRelationships: Ally, Neutral, Enemy
		Color: FFFFFF

combobulate.file.docs:
	Inherits: DOME
	RenderSprites:
		Image: dome
	-Buildable:
	Power:
		Amount: 0
	CombobulateLabel:
		Font: TinyBold
		Position: Top
		RequiresSelection: false
		ValidRelationships: Ally, Neutral, Enemy
		Color: FFFFFF

combobulate.file.build:
	Inherits: SILO
	RenderSprites:
		Image: silo
	-Buildable:
	Power:
		Amount: 0
	CombobulateLabel:
		Font: TinyBold
		Position: Top
		RequiresSelection: false
		ValidRelationships: Ally, Neutral, Enemy
		Color: FFFFFF
