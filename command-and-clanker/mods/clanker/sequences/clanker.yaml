clankerterminal:
	Inherits: fact
	# The Terminal keeps the construction-yard body but takes a custom, unique
	# build cameo (a user-provided PNG) so it does not collide with another
	# building's icon in the palette.
	icon:
		Filename: clanker|uibits/terminal-icon.png
